name: BTC Signal Bot

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Test GitHub Actions
        run: |
          echo "BTC Signal Bot - workflow funcionando correctamente"
          echo "Fecha:"
          date
